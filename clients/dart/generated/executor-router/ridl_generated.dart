// Generated by ridl from executor-router.route-map.json. DO NOT EDIT.
// Edit the route map and re-run `ridl generate`; a drift check fails the build if this file and the map disagree.
//
// service: gha-executor-router
// version: 0.2.0
//
// Scalar newtypes are extension types: free at runtime, distinct to the analyzer.

// ignore_for_file: unnecessary_cast, unused_element

import 'dart:convert';

const String kService = "gha-executor-router";
const String kVersion = "0.2.0";

// ---------------------------------------------------------------- types

/// A namespaced build id, `<executor>~<upstream job id>`. The executor is pinned into the id so status reads never fan out.
extension type const BuildRouteId(String value) implements Object {}

class ExecutorRef {
  const ExecutorRef({required this.id, required this.provider});

  factory ExecutorRef.fromJson(Map<String, Object?> json) {
    return ExecutorRef(
      id: json["id"] as String,
      provider: json["provider"] as String,
    );
  }

  final String id;
  /// aws | hetzner.
  final String provider;

  Map<String, Object?> toJson() {
    return <String, Object?>{
      "id": id,
      "provider": provider,
    };
  }
}

class RouterHealth {
  const RouterHealth({required this.ok, required this.service, required this.executionEnabled, required this.configuredExecutors, required this.enabledExecutors, required this.authConfigured, required this.retainedAssignments, required this.maxAssignments});

  factory RouterHealth.fromJson(Map<String, Object?> json) {
    return RouterHealth(
      ok: json["ok"] as bool,
      service: json["service"] as String,
      executionEnabled: json["executionEnabled"] as bool,
      configuredExecutors: json["configuredExecutors"] as int,
      enabledExecutors: json["enabledExecutors"] as int,
      authConfigured: json["authConfigured"] as bool,
      retainedAssignments: json["retainedAssignments"] as int,
      maxAssignments: json["maxAssignments"] as int,
    );
  }

  final bool ok;
  final String service;
  final bool executionEnabled;
  final int configuredExecutors;
  final int enabledExecutors;
  final bool authConfigured;
  final int retainedAssignments;
  final int maxAssignments;

  Map<String, Object?> toJson() {
    return <String, Object?>{
      "ok": ok,
      "service": service,
      "executionEnabled": executionEnabled,
      "configuredExecutors": configuredExecutors,
      "enabledExecutors": enabledExecutors,
      "authConfigured": authConfigured,
      "retainedAssignments": retainedAssignments,
      "maxAssignments": maxAssignments,
    };
  }
}

/// 200 with an empty `readyExecutors` when execution is disabled; otherwise 200 only if at least one executor's /readyz probe succeeded, else 503.
class RouterReady {
  const RouterReady({required this.ok, required this.service, required this.executionEnabled, required this.executionReady, required this.readyExecutors});

  factory RouterReady.fromJson(Map<String, Object?> json) {
    return RouterReady(
      ok: json["ok"] as bool,
      service: json["service"] as String,
      executionEnabled: json["executionEnabled"] as bool,
      executionReady: json["executionReady"] as bool,
      readyExecutors: json["readyExecutors"] == null ? const [] : (json["readyExecutors"] as List<Object?>).map((e) => ExecutorRef.fromJson(e as Map<String, Object?>)).toList(growable: false),
    );
  }

  final bool ok;
  final String service;
  final bool executionEnabled;
  final bool executionReady;
  final List<ExecutorRef> readyExecutors;

  Map<String, Object?> toJson() {
    return <String, Object?>{
      "ok": ok,
      "service": service,
      "executionEnabled": executionEnabled,
      "executionReady": executionReady,
      "readyExecutors": readyExecutors.map((e) => e.toJson()).toList(growable: false),
    };
  }
}

/// Why the router never fails over after a POST: an ambiguous acceptance could duplicate work.
class RouterRouting {
  const RouterRouting({required this.order, required this.preSubmissionFailover, required this.postSubmissionFailover, required this.reason, required this.statusPinning});

  factory RouterRouting.fromJson(Map<String, Object?> json) {
    return RouterRouting(
      order: json["order"] as String,
      preSubmissionFailover: json["preSubmissionFailover"] as String,
      postSubmissionFailover: json["postSubmissionFailover"] as bool,
      reason: json["reason"] as String,
      statusPinning: json["statusPinning"] as String,
    );
  }

  final String order;
  final String preSubmissionFailover;
  final bool postSubmissionFailover;
  final String reason;
  final String statusPinning;

  Map<String, Object?> toJson() {
    return <String, Object?>{
      "order": order,
      "preSubmissionFailover": preSubmissionFailover,
      "postSubmissionFailover": postSubmissionFailover,
      "reason": reason,
      "statusPinning": statusPinning,
    };
  }
}

class RouterCoordination {
  const RouterCoordination({required this.requestIdForwardedUnchanged, required this.requestContentBinding, required this.ambiguousAssignmentRetention, required this.assignmentScope, required this.crossProviderResubmissionRequires});

  factory RouterCoordination.fromJson(Map<String, Object?> json) {
    return RouterCoordination(
      requestIdForwardedUnchanged: json["requestIdForwardedUnchanged"] as bool,
      requestContentBinding: json["requestContentBinding"] as bool,
      ambiguousAssignmentRetention: json["ambiguousAssignmentRetention"] as bool,
      assignmentScope: json["assignmentScope"] as String,
      crossProviderResubmissionRequires: json["crossProviderResubmissionRequires"] as String,
    );
  }

  final bool requestIdForwardedUnchanged;
  final bool requestContentBinding;
  final bool ambiguousAssignmentRetention;
  final String assignmentScope;
  final String crossProviderResubmissionRequires;

  Map<String, Object?> toJson() {
    return <String, Object?>{
      "requestIdForwardedUnchanged": requestIdForwardedUnchanged,
      "requestContentBinding": requestContentBinding,
      "ambiguousAssignmentRetention": ambiguousAssignmentRetention,
      "assignmentScope": assignmentScope,
      "crossProviderResubmissionRequires": crossProviderResubmissionRequires,
    };
  }
}

class RouterCapabilities {
  const RouterCapabilities({required this.service, required this.schemaVersion, required this.executionEnabled, required this.executors, required this.acceptedJobKind, required this.immutableRevisionRequired, required this.routing, required this.coordination});

  factory RouterCapabilities.fromJson(Map<String, Object?> json) {
    return RouterCapabilities(
      service: json["service"] as String,
      schemaVersion: json["schemaVersion"] as String,
      executionEnabled: json["executionEnabled"] as bool,
      executors: json["executors"] == null ? const [] : (json["executors"] as List<Object?>).map((e) => ExecutorRef.fromJson(e as Map<String, Object?>)).toList(growable: false),
      acceptedJobKind: json["acceptedJobKind"] as String,
      immutableRevisionRequired: json["immutableRevisionRequired"] as bool,
      routing: RouterRouting.fromJson(json["routing"] as Map<String, Object?>),
      coordination: RouterCoordination.fromJson(json["coordination"] as Map<String, Object?>),
    );
  }

  final String service;
  /// "gha-executor-router.v1".
  final String schemaVersion;
  final bool executionEnabled;
  final List<ExecutorRef> executors;
  /// Always "run-profile".
  final String acceptedJobKind;
  final bool immutableRevisionRequired;
  final RouterRouting routing;
  final RouterCoordination coordination;

  Map<String, Object?> toJson() {
    return <String, Object?>{
      "service": service,
      "schemaVersion": schemaVersion,
      "executionEnabled": executionEnabled,
      "executors": executors.map((e) => e.toJson()).toList(growable: false),
      "acceptedJobKind": acceptedJobKind,
      "immutableRevisionRequired": immutableRevisionRequired,
      "routing": routing.toJson(),
      "coordination": coordination.toJson(),
    };
  }
}

/// The only body POST /builds accepts, per `validate_build_request` in src/executor_router.rs. Any other key is rejected. It is not declared as this route's `request` because the handler takes the raw `Bytes` body: the request bytes are content-bound to the assignment for duplicate detection.
class RouterBuildRequest {
  const RouterBuildRequest({required this.schemaVersion, required this.jobKind, required this.repoUrl, required this.gitRef, required this.profile, required this.requestId});

  factory RouterBuildRequest.fromJson(Map<String, Object?> json) {
    return RouterBuildRequest(
      schemaVersion: json["schemaVersion"] as String,
      jobKind: json["jobKind"] as String,
      repoUrl: json["repoUrl"] as String,
      gitRef: json["gitRef"] as String,
      profile: json["profile"] as String,
      requestId: json["requestId"] as String,
    );
  }

  /// Must equal "build-server.v1".
  final String schemaVersion;
  /// Must equal "run-profile".
  final String jobKind;
  final String repoUrl;
  /// A lowercase 40-hex commit SHA.
  final String gitRef;
  /// Lowercase letters, digits, and hyphens.
  final String profile;
  /// 1-256 chars of letters, digits, '.', ':', '_', '-'. Forwarded upstream unchanged.
  final String requestId;

  Map<String, Object?> toJson() {
    return <String, Object?>{
      "schemaVersion": schemaVersion,
      "jobKind": jobKind,
      "repoUrl": repoUrl,
      "gitRef": gitRef,
      "profile": profile,
      "requestId": requestId,
    };
  }
}

// ------------------------------------------------------------- transport

/// How a call reaches the server.
enum Delivery {
  direct('direct'),
  optoSyncQueued('opto_sync_queued');

  const Delivery(this.wire);

  final String wire;
}

/// Where a queued call's opto-sync record id comes from.
enum RecordIdFrom {
  path, request, minted;
}

/// Static description of a queued call's opto-sync binding.
class OptoSyncBinding {
  const OptoSyncBinding({required this.table, required this.operation, required this.from, this.name});

  final String table;
  final String operation;
  final RecordIdFrom from;
  final String? name;
}

/// One outbound call, fully resolved. Transports never re-derive paths.
class RpcRequest {
  const RpcRequest({
    required this.key,
    required this.method,
    required this.path,
    required this.pathTemplate,
    this.query = const [],
    this.body,
    this.delivery = Delivery.direct,
    this.optoSync,
  });

  final String key;
  final String method;

  /// Path with every parameter already substituted and encoded.
  final String path;

  /// The unsubstituted template, so a transport can locate a named
  /// parameter inside [path] instead of guessing at its position.
  final String pathTemplate;
  final List<MapEntry<String, String>> query;

  /// JSON body, or null for operations that carry none.
  final String? body;
  final Delivery delivery;
  final OptoSyncBinding? optoSync;
}

/// The single seam between generated code and the network.
///
/// Implement it over HTTP for direct calls and over an opto-sync queue for
/// queued ones. The dependency runs one way: this library calls opto-sync,
/// never the reverse.
abstract interface class RpcTransport {
  Future<String> call(RpcRequest request);
}

/// Encode one path segment so an id containing `/` cannot reshape the URL.
String encodeSegment(String value) => Uri.encodeComponent(value);

String _queryValue(Object? value) => value is String ? value : '$value';

// ------------------------------------------------------------- operations

/// `GET /`
String descriptorPath() => "/";

/// `GET /healthz`
String healthzPath() => "/healthz";

/// `GET /readyz`
String readyzPath() => "/readyz";

/// `GET /v1/capabilities`
String capabilitiesPath() => "/v1/capabilities";

/// `POST /builds`
String submitBuildPath() => "/builds";

/// `GET /builds/{id}`
String getBuildPath(BuildRouteId id) =>
  '/builds/${encodeSegment('$id')}';

/// Self-describing service document.
Future<Object?> descriptor(RpcTransport transport) async {
  final path = descriptorPath();
  const query = <MapEntry<String, String>>[];
  final raw = await transport.call(RpcRequest(
    key: "descriptor",
    method: "GET",
    path: path,
    pathTemplate: "/",
    query: query,
    delivery: Delivery.direct,
  ));
  return jsonDecode(raw);
}

/// Liveness plus configured and enabled executor counts.
Future<RouterHealth> healthz(RpcTransport transport) async {
  final path = healthzPath();
  const query = <MapEntry<String, String>>[];
  final raw = await transport.call(RpcRequest(
    key: "healthz",
    method: "GET",
    path: path,
    pathTemplate: "/healthz",
    query: query,
    delivery: Delivery.direct,
  ));
  return RouterHealth.fromJson(jsonDecode(raw) as Map<String, Object?>);
}

/// Readiness, probing every enabled executor.
Future<RouterReady> readyz(RpcTransport transport) async {
  final path = readyzPath();
  const query = <MapEntry<String, String>>[];
  final raw = await transport.call(RpcRequest(
    key: "readyz",
    method: "GET",
    path: path,
    pathTemplate: "/readyz",
    query: query,
    delivery: Delivery.direct,
  ));
  return RouterReady.fromJson(jsonDecode(raw) as Map<String, Object?>);
}

/// Placement policy, enabled executors, and the fail-closed coordination rules.
Future<RouterCapabilities> capabilities(RpcTransport transport) async {
  final path = capabilitiesPath();
  const query = <MapEntry<String, String>>[];
  final raw = await transport.call(RpcRequest(
    key: "capabilities",
    method: "GET",
    path: path,
    pathTemplate: "/v1/capabilities",
    query: query,
    delivery: Delivery.direct,
  ));
  return RouterCapabilities.fromJson(jsonDecode(raw) as Map<String, Object?>);
}

/// Place one fixed-profile build on an executor and relay the upstream answer.
Future<Object?> submitBuild(RpcTransport transport) async {
  final path = submitBuildPath();
  const query = <MapEntry<String, String>>[];
  final raw = await transport.call(RpcRequest(
    key: "submit_build",
    method: "POST",
    path: path,
    pathTemplate: "/builds",
    query: query,
    delivery: Delivery.direct,
  ));
  return jsonDecode(raw);
}

/// Status of one placed build, read from the executor pinned in the id.
Future<Object?> getBuild(RpcTransport transport, BuildRouteId id) async {
  final path = getBuildPath(id);
  const query = <MapEntry<String, String>>[];
  final raw = await transport.call(RpcRequest(
    key: "get_build",
    method: "GET",
    path: path,
    pathTemplate: "/builds/{id}",
    query: query,
    delivery: Delivery.direct,
  ));
  return jsonDecode(raw);
}

// -------------------------------------------------------------- manifest

/// One row per declared operation.
class OperationInfo {
  const OperationInfo({required this.key, required this.path, required this.methods, required this.delivery});

  final String key;
  final String path;
  final List<String> methods;
  final Delivery delivery;
}

/// Every operation in the route map, in declaration order.
const List<OperationInfo> operations = [
  OperationInfo(key: "descriptor", path: "/", methods: ["GET"], delivery: Delivery.direct),
  OperationInfo(key: "healthz", path: "/healthz", methods: ["GET"], delivery: Delivery.direct),
  OperationInfo(key: "readyz", path: "/readyz", methods: ["GET"], delivery: Delivery.direct),
  OperationInfo(key: "capabilities", path: "/v1/capabilities", methods: ["GET"], delivery: Delivery.direct),
  OperationInfo(key: "metrics", path: "/metrics", methods: ["GET"], delivery: Delivery.direct),
  OperationInfo(key: "submit_build", path: "/builds", methods: ["POST"], delivery: Delivery.direct),
  OperationInfo(key: "get_build", path: "/builds/{id}", methods: ["GET"], delivery: Delivery.direct),
];

/// Operations that route through opto-sync's durable queue.
const List<String> queuedOperations = [];
